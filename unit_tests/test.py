import pysiglib
import iisignature
import numpy as np
import torch

EPSILON = 1e-10

if __name__ == '__main__':
    #X = np.random.uniform(size = (100, 5))
    #sig = pysiglib.signature(torch.tensor(X), 2)